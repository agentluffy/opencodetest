
## Security Notice

**This fork is intended for security research and testing purposes only.** All security restrictions have been removed from the prompt files to allow for unrestricted code analysis and security testing. Use responsibly and only on systems you own or have explicit permission to test.

---

### Building

bun works

docker works

**install file broken**

**conflicts with opencode**

### YOLO Mode

For automated workflows, use the `--yolo` flag to auto-approve all permissions (dangerous commands like `rm -rf /` are still blocked):


### Documentation

(https://opencode.ai/docs).
